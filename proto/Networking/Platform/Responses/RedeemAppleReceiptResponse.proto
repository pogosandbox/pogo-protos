syntax = "proto3";
package POGOProtos.Networking.Platform.Responses;

message RedeemAppleReceiptResponse {
	repeated string provisioned_transaction_tokens = 1;

	enum Status {
		UNSET = 0;
		SUCCESS = 1;
		FAILURE = 2;
	}
}
